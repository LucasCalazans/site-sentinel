{
    "name": "site-sentinel",
    "version": "0.1.0",
    "private": true,
    "description": "Monitor periódico de sites — checks plugáveis (integridade, redirect chain, defacement, performance) rodando em Cloudflare Worker com cron triggers e alerting via Discord webhook.",
    "type": "module",
    "scripts": {
        "deploy": "wrangler deploy",
        "dev": "wrangler dev",
        "typecheck": "tsc --noEmit",
        "test": "vitest run",
        "test:watch": "vitest"
    },
    "devDependencies": {
        "@cloudflare/workers-types": "^4.20250101.0",
        "typescript": "^5.5.0",
        "vitest": "^2.1.0",
        "wrangler": "^3.95.0"
    }
}
